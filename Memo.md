# 迭代记录

1. 初版，完成“项目拷问官”整体代码构建，利用skills调用qwen模型来实现对简历中所提供的项目的深度问答和对回答的评分

2. 加入了压力条，用于在文字问答中模拟现实面试时候的紧张感，有进度条和时间提醒

3. 进一步优化agent skill的调用，让api自主决策当前轮问答的时间限制以及问答轮数，并优化AI面试官的语气和问题层次更像真人

4. 加入模式选择（实时看到反馈和完整多轮问答结束后反馈），可以更好的帮助不同类型的用户进行训练和模拟（实时反馈和整体反馈）

5. 加入用户策略，在服务器端记录用户对话记录和log，方便用户查询历史记录；优化反馈prompt。

6. 优化prompt，让调用的模型敢于质疑和否定，而不是一味的深入询问，更像真人一点；加入流式输出，不用干等模型全部结果返回；新增面试官风格选择，不再让问答中全部都是专业词汇，更符合真人。

7. 解决bug，当问题和答案完全不匹配的时候，模型会识别，但是分数仍然很高，同时显示当轮评分和平均分

8. 整理项目结构；加入等待时长的评分，模拟真实场景下的等待时间；明确报告边界，所有分析结果都可以在报告中查看